import { FilterMovesPipe } from './filter-moves.pipe';

describe('FilterMovesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterMovesPipe();
    expect(pipe).toBeTruthy();
  });
});
